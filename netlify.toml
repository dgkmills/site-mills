This configuration file tells Netlify how to build and deploy your main site.It now runs the screenshot script before deploying the site.[build]This command installs the node-fetch dependency and runs the screenshot script.command = "npm install --production && node screenshot.js"publish = "."Form handling settings[build.processing]skip_processing = false[build.processing.css]bundle = trueminify = true[build.processing.js]bundle = trueminify = true[build.processing.html]pretty_urls = true[build.processing.images]compress = true