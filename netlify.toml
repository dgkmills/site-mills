This configuration file tells Netlify how to build and deploy your main site.Place this in the root of your "Site Mills" project folder.[build]We are switching to a much lighter build command using a Node.js fetch script.This drastically reduces build time and preserves your Netlify build minutes.command = "npm install --production && node screenshot.js"publish = "."Form handling settings[build.processing]skip_processing = false[build.processing.css]bundle = trueminify = true[build.processing.js]bundle = trueminify = true[build.processing.html]pretty_urls = true[build.processing.images]compress = true